import styled from 'styled-components';

export const Icon = styled.a`
  display: flex;
  justify-content: flex-end;
  position: fixed;
  z-index: 100;
  right: 20px;
  cursor: pointer;

  > img {
    width: 50px;
    height: 50px;
  }
`;
